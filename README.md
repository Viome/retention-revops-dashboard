# Retention RevOps Scorecard — temporary public host

⚠️ **Temporary.** Public GitHub Pages mirror of an **internal Viome** retention scorecard, created
for easy sharing/embedding. It contains internal business metrics and should be **moved to a
private / internal (SSO or IP-allowlisted) host** and this public repo **deleted** once that's
available.

- Live page: enabled via GitHub Pages (root of `main`).
- 6 top metrics (Total Revenue = core) over live Shopify data + retention-roadmap initiatives.
- Source lives in `Desktop/Claude Stuff/retention-revops-dashboard.html` (only `index.html` published — no secrets).
- Static snapshot — refresh by re-pulling the Shopify data and re-deploying.
